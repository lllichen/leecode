package ink.lichen.leecode.medium;

public class Search {

    public int search(int[] nums, int target) {
        int flag = nums[0];

        if (target == flag){
            return 0;
        }
        int n = nums.length;
        int lo=0,high=n-1;
        while (lo <= high){
            int mid = lo + high;
        }

        return 0;
    }
}
